<ul>
  <% @index.each do |filename| %>
    <li>
      <a href="/<%= filename %>"><%= filename %></a> - 
      <% unless SUPPORTED_IMAGETYPES.include?(File.extname(filename)) %>
      <a href="/<%= filename %>/edit">Edit</a>
      <% end %>
      <form class="inline" action="/<%= filename %>/duplicate" method="post" >
        <button type="submit">Duplicate</button>
      </form>
      <form class="inline" action="/<%= filename %>/delete" method="post" >
        <button type="submit">Delete</button>
      </form>
    </li>
  <% end %>
</ul>
<p><a href="/new">New Document</a></p>
<p><a href="/upload">Upload Document</a></p>
<% if signed_in? %>
  <form class="inline" action="/users/signout" method="post" >
    <p class="user-status">
      Signed in as <%= session[:username] %>.
      <button type="submit">Sign Out</button>
    </p>
  </form>
<% else %>
  <form class="inline" action="/users/signin" method="get" >
    <button type="submit">Sign In</button>
  </form>

  <form class="inline" action="/users/signup" method="get" >
    <button type="submit">Sign Up</button>
  </form>
<% end %>
